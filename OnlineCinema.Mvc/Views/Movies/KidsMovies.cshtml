@model IEnumerable<KidsViewModel>

<div class="row row-cols-1 row-cols-md-4 g-4 p-3">
    @foreach (var movie in Model)
    {
        <div class="col">
            <div class="card card-movie-poster h-100 custom-card">
                <a asp-controller="Movies" asp-action="Get" asp-route-id="@movie.Id">
                    <img src="@movie.CardImagePath" class="card-img-custom" alt="Some picture">
                </a>
                <div class="card-body">
                    <p class="card-text">@movie.AgeRating+</p>
                </div>
                <div class="overlay">
                    <div class="movie-title">@movie.Name</div>
                </div>
            </div>
        </div>
    }
</div>